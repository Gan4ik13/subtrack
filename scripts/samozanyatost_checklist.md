# Самозанятость: легализация приёма оплат SubTrack

Доход от подписок (15 ₽/мес) для самозанятого — налог на профессиональный доход
(НПД): **4% от физлиц**, **6% от юрлиц**. Лимит — 2,4 млн ₽/год.
На каждый платёж от пользователя нужно сформировать **чек** и передать его в ФНС.

## 1. Зарегистрироваться как самозанятый (однократно, 15 минут)

1. Приложение **«Мой налог»** (ФНС) — по паспорту или через Госуслуги.
2. Подтвердите регистрацию — автоматически получите ИНН.
3. Готово: сможете формировать чеки и платить налог в этом приложении.

## 2. Авто-чеки на ЮMoney (главный шаг — без него каждый чек вручную)

Сейчас деньги приходят на кошелёк по «Переводу по кнопке» (quickpay-форма).
Такие переводы — тоже облагаемый доход, если это оплата услуги. Чтобы чеки
формировались автоматически, подключите кошелёк к сервису самозанятых ЮMoney:

- **ЮSelf** (сервис ЮMoney для самозанятых): https://yoomoney.ru/yoself
- Войдите по своему кошельку → подтвердите статус самозанятого через «Мой налог»
  (или регистрация прямо в сервисе) → включите авто-чеки для входящих переводов.
- ЮSelf сам создаёт чеки по каждому зачислению, считает налог 4/6% и умеет его
  платить. Это **закрывает требование ФНС автоматически** для текущего флоу.

Если ЮSelf не работает с переводами по кнопке (внимательно проверьте в интерфейсе),
альтернатива — формировать чеки вручную в «Мой налог» на каждую оплату:
сумма 15 ₽, «Доход от физлица», комментарий с email пользователя.

## 3. (Рекомендуется на рост) ЮKassa как самозанятый

ЮKassa официально принимает самозанятых и **автоматически генерирует чеки ФНС**
и отправляет электронный чек покупателю. Плюс карты/SBP/Т-Пэй, аналитика.
Минус — нужно переписать платёжный флоу (создание платежа через API ЮKassa,
конфирмация по вебхуку/поллингу). Сейчас это НЕ требуется: ЮSelf закрывает вопрос.

## 4. Что уже автоматизировано в коде (сделано)

- **Чек владельцу**: при каждой оплате бот пишет владельцу в Telegram
  (`OWNER_TG_CHAT_ID`) — email, сумма, до какого числа активен Premium.
- **Пользователю**: при активации Premium бот пишет ему подтверждение
  (если привязан `telegram_chat_id`).
- **Лог платежей**: метаданные вебхука (operation_id, sender) сохраняются в БД;
  владелец видит список последних 100 платежей в разделе «Платежи» настроек
  (аккаунт с email, равным `OWNER_EMAIL`).

## 5. Переменные окружения на Render (задать вручную)

- `OWNER_TG_CHAT_ID` — ваш числовой ID в Telegram (узнать у @userinfobot).
- `OWNER_EMAIL` — email вашего аккаунта на сайте (тот, под которым входите как владелец).

Поставить: `scripts/render_env.ps1` добавляет переменные и перезапускает деплой,
либо вручную в Render dashboard → Environment → Save → Manual Deploy.

## Итог для налоговой

- Доход: платите 4% через ЮSelf/«Мой налог» автоматически.
- Чеки: авто (ЮSelf) или вручную («Мой налог»).
- Контроль: лог в настройках сайта + Telegram-уведомления на каждый платёж.
